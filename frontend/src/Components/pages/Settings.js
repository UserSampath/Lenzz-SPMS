import SideBar from "./mainComponents/Sidebar";

const Settings = () => {
  return (
    <SideBar>
      <div className="pageContainer">
        <h1>Settings</h1>
      </div>
    </SideBar>
  );
};

export default Settings;
