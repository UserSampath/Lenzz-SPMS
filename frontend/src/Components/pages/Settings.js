import SideBar from "./Sidebar";

const Settings = () => {
  return (
    <SideBar>
      <div className="pageContainer">
        <h1>Settings</h1>
      </div>
    </SideBar>
  );
};

export default Settings;
